name: "Setup Heady"
description: "Standardizes Node 22 LTS and PNPM 9.15.0 across all Heady pipelines"
runs:
  using: "composite"
  steps:
    - name: Install pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 9.15.0
        standalone: true
    - name: Setup Node 22 LTS
      uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: 'pnpm'
